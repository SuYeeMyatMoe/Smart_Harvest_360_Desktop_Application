# MySQL defaults for SmartHarvest 360 (optional — app still runs on CSV if offline).
# Put the real password in project-root .env as DB_PASSWORD (see .env.example).
jdbc.url=jdbc:mysql://localhost:3306/smartharvest?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
jdbc.user=root
jdbc.password=DB_PASSWORD
