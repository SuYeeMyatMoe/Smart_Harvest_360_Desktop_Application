# MySQL connection for SmartHarvest 360 (optional — app still runs on CSV if offline)
jdbc.url=jdbc:mysql://localhost:3306/smartharvest?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&connectTimeout=3000&socketTimeout=3000
jdbc.user=root
jdbc.password=
