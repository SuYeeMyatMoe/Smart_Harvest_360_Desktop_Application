# Copy this file to .env and fill in your MySQL password.
# The app loads .env from the project root (optional — CSV mode works without it).

DB_URL=jdbc:mysql://localhost:3306/smartharvest?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USER=root
DB_PASSWORD=YOUR_PASSWORD
